spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/keepbooking
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.keepbooking: DEBUG
    org.springframework.security: DEBUG
